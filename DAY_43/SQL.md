Today we look at how to query "Parent-Child" data (like an Org Chart).

Topic: Recursive CTEs.

Concept: A CTE that references itself.

Task: Write a query to find all managers and their direct/indirect reports in an Employees table (Hierarchical query).

Interview Question: "What is the terminating condition in a Recursive CTE?" (The part where the query stops calling itself).