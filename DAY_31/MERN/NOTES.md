# Mongoose & Backend Logic
- Schema: Defines the structure of the document.
- Model: Provides the interface to the database (CRUD).
- Using `bcrypt` logic (preview) inside models for password hashing is the professional standard.